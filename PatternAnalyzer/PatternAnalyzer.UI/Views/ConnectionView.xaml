<UserControl x:Class="PatternAnalyzer.UI.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:PatternAnalyzer.Core.Models;assembly=PatternAnalyzer.Core"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <GroupBox Header="Connection Settings" Margin="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Type:"/>
            <ComboBox Grid.Row="0" Grid.Column="1" Margin="2"
                      ItemsSource="{Binding DatabaseTypes}"
                      SelectedItem="{Binding SelectedDatabaseType}"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Host:"/>
            <TextBox Grid.Row="1" Grid.Column="1" Margin="2" Text="{Binding Host}"/>

            <Label Grid.Row="2" Grid.Column="0" Content="Port:"/>
            <TextBox Grid.Row="2" Grid.Column="1" Margin="2" Text="{Binding Port}"/>

            <Label Grid.Row="3" Grid.Column="0" Content="DB/Service:"/>
            <TextBox Grid.Row="3" Grid.Column="1" Margin="2" Text="{Binding DatabaseName}"/>

            <Label Grid.Row="4" Grid.Column="0" Content="Username:"/>
            <TextBox Grid.Row="4" Grid.Column="1" Margin="2" Text="{Binding Username}"/>

            <Label Grid.Row="5" Grid.Column="0" Content="Password:"/>
            <TextBox Grid.Row="5" Grid.Column="1" Margin="2" Text="{Binding Password}"/>

            <Button Grid.Row="6" Grid.Column="1" Margin="2" Content="Connect" Command="{Binding ConnectCommand}"/>
            <TextBlock Grid.Row="6" Grid.Column="0" Margin="2" Text="{Binding StatusMessage}" Foreground="Gray"/>

            <Grid Grid.Row="7" Grid.ColumnSpan="2" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <GroupBox Header="Tables" Grid.Column="0">
                    <ListBox ItemsSource="{Binding Tables}" SelectedItem="{Binding SelectedTable}" DisplayMemberPath="Name"/>
                </GroupBox>
                <GroupBox Header="Columns" Grid.Column="1">
                    <ListBox ItemsSource="{Binding Columns}" SelectedItem="{Binding SelectedColumn}" DisplayMemberPath="Name"/>
                </GroupBox>
            </Grid>
        </Grid>
    </GroupBox>
</UserControl>
